<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>EPCarpool | Login</title>
  
  <!-- Modern Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  
  <!-- Stylesheets -->
  <link href="./bulma.min.css" rel="stylesheet" type="text/css" />
  <link href="./style.css" rel="stylesheet" type="text/css" />
  
  <!-- Icons -->
  <script src="https://kit.fontawesome.com/16ae8e710d.js" crossorigin="anonymous"></script>
  <link rel="icon" href="favicon.png" type="image/x-icon">
</head>

<body>
  <main class="hero is-fullheight flex items-center justify-center">
    <% if (error) { %>
      <div class="message is-danger fade-in" style="position: fixed; top: 2rem; left: 50%; transform: translateX(-50%); z-index: 1000; max-width: 400px;">
        <div class="message-header">
          <p><i class="fas fa-exclamation-triangle mr-2"></i>Warning</p>
          <button class="delete" aria-label="delete" onclick="this.parentElement.parentElement.remove();"></button>
        </div>
        <div class="message-body">
          <%= error %>
        </div>
      </div>
    <% } %>

    <% if (message) { %>
      <div class="message is-success fade-in" style="position: fixed; top: 2rem; left: 50%; transform: translateX(-50%); z-index: 1000; max-width: 400px;">
        <div class="message-header">
          <p><i class="fas fa-check-circle mr-2"></i>Success</p>
          <button class="delete" aria-label="delete" onclick="this.parentElement.parentElement.remove();"></button>
        </div>
        <div class="message-body">
          <%= message %>
        </div>
      </div>
    <% } %>

    <div class="auth-panel fade-in">
      <div class="auth-panel-header">
        <img class="auth-panel-logo floating" src="EPS Logo - Transparent-4.png" alt="EPCarpool Logo">
        <h1 class="auth-panel-title">Welcome to EPCarpool</h1>
        <p class="text-sm opacity-75 mt-2">The one place to keep track of all EPS events</p>
      </div>

      <div class="mb-6 p-4 bg-white bg-opacity-20 rounded-lg border border-white border-opacity-20">
        <p class="text-center text-sm leading-relaxed">
          Create and sign up for carpools with ease. Connect with your EPS community and travel together sustainably.
        </p>
      </div>

      <button class="button button-primary w-full button-large" id="sign-in">
        <i class="fas fa-graduation-cap mr-2"></i>
        Sign in with @EASTSIDEPREP.ORG
      </button>

      <div class="mt-6 text-center">
        <a href="/sustainabilityStatement" class="text-sm opacity-75 hover:opacity-100 transition-opacity flex items-center justify-center">
          <i class="fas fa-leaf mr-2 text-green-400"></i>
          View our sustainability statement
        </a>
      </div>
    </div>
  </main>
  
  <script type="module" src="./signin.js"></script>
</body>

</html>
